<%- include("../partials/header.ejs") %>
<script type="application/ld+json">
    {
        "@context": "http://schema.org",
        "@type": "BreadcrumbList",
        "itemListElement": [
            {
                "@type": "ListItem",
                "position": 1,
                "name": "Home",
                "item": "https://discordextremelist.xyz/"
            }
        ]
    }
</script>
<%- include("../partials/nav.ejs") %>
<section class="hero is-black">
    <div class="hero-body">
        <br />
        <div class="container has-text-centered">
            <img class="backdrop" draggable=false alt="DEL Logo" style="padding: 5px;" src="/img/logo-128px.<%= imageFormat %>" type="image/<%= imageFormat %>" draggable="false">
            <h1 class="title">
                <%= __("common.del") %>
            </h1>
            <h2 class="subtitle">
                <%= __("common.del.subtitle") %>
            </h2>
        </div>
    </div>
</section>
<div class="container">
    <div class="section">
        <% if (bots.length === 0) { %>
            <h2 class="is-size-3"><%= __("common.error.fetchFail.bots") %></h2>
        <% } else { %>
            <h2 class="is-size-3 has-text-centered has-text-blurple">
                <span onclick="invokeSeizureMode();" class="icon is-medium iconalign">
                    <i class="fad fa-robot fa-1x"></i>
                </span>
                <span><%= __("Random Bots") %></span>
            </h2>
            <br />
            <div class="columns is-multiline is-centered">
                <%- bots.map(bot => include("../partials/cards/botCard.ejs", { bot, search: false, queue: false, archived: false, verificationApp: false })).join("") %>
            </div>
        <% } %>
    </div>
    <div class="section">
        <% if (servers.length === 0) { %>
            <h2 class="is-size-3"><%= __("common.error.fetchFail.servers") %></h2>
        <% } else { %>
            <h2 class="is-size-3 has-text-centered has-text-blurple">
                <span class="icon is-medium iconalign">
                    <i class="fad fa-comments-alt fa-1x"></i>
                </span>
                <span><%= __("Random Servers") %></span>
            </h2>
            <br />
            <div class="columns is-multiline is-centered">
                <%- servers.map(server => include("../partials/cards/serverCard.ejs", { server, search: false })).join("") %>
            </div>
        <% } %>
    </div>
</div>    
<%- include("../partials/footer.ejs") %>